{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\" ?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range .Site.RegularPages }}
    {{- $heroURL := "" }}
    {{- $heroType := "" }}
    {{- if isset .Params "hero" }}
      {{- $heroURL = .Params.hero }}
    {{- else }}
      {{- with .Resources.GetMatch "hero*" }}
        {{- $heroURL = .Permalink }}
        {{- $heroType = .MediaType.Type }}
      {{- end }}
    {{- end }}
    <item>
      <title>{{ .Title | transform.XMLEscape | safeHTML }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      <description>{{ .Content | transform.XMLEscape | safeHTML }}</description>
      <atom:summary>{{ with .Description }}{{ . | plainify | transform.XMLEscape | safeHTML }}{{ else }}{{ .Summary | plainify | transform.XMLEscape | safeHTML }}{{ end }}</atom:summary>
      {{- with $heroURL }}
      {{ printf "<atom:link rel=\"enclosure\" href=%q%s />" . (cond (ne $heroType "") (printf " type=%q" $heroType) "") | safeHTML }}
      {{- end }}
    </item>
    {{- end }}
  </channel>
</rss>
